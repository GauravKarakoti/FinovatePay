import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  initialMessages: [createChatBotMessage(`Welcome to FinovatePay! How can I help you today?`)],
  botName: 'FinovateBot',
};

export default config;